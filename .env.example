EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Server/script-only — bypasses RLS. Never prefix with EXPO_PUBLIC_, never
# ship in the app bundle. Used only by scripts/test-rls.ts and future Edge
# Functions. Project Settings -> API -> service_role key.
SUPABASE_SERVICE_ROLE_KEY=