import { ComingSoonScreen } from '@/screens/ComingSoonScreen';

// TODO: real messaging — explicitly out of scope for the app-shell build.
export default function MessagesScreen() {
  return <ComingSoonScreen title="Messages" icon="comment-dots" />;
}
