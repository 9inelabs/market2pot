import { ComingSoonScreen } from '@/screens/ComingSoonScreen';

// TODO: real search — explicitly out of scope for the app-shell build.
export default function SearchScreen() {
  return <ComingSoonScreen title="Search" icon="search" />;
}
